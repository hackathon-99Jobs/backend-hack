export type Estudante = {
    id: string
    nome: string
    email: string
    cpf: string
    redacao: string
    senha: string
  };
  export type Empresa = {
    id: string
    nomeEmpresa: string
    email: string
    telefone: string
    cnpj: string
  };